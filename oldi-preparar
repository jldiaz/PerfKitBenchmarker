if [ $# -lt 1  ]
  then
    echo "Falta especificar el run uri"
    exit 1
fi
./pkb.py --run_stage=prepare --timing_measurements=runtimes --cloud=Azure --benchmarks=qps_oldisim --qps_oldisim_num_leaves=1 --run_uri=$1

